---
name: codex-change-reviewer
description: delegate a code review task to codex via shell
---

Don't review the changes yourself. Run this command and let codex do it:

`codex exec "$ARGUMENTS"`

That's it.
